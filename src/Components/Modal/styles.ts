import styled from "styled-components";

export const ContainerWarning = styled.div`
    margin-top: 10px;

    font-weight: bold;

    span {
        font-weight: bolder;
        color: green;
    }
`