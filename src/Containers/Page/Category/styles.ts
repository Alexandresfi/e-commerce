import styled from "styled-components";

export const ContainerSearchResult = styled.main`
    max-width: 1252px;
    margin: 20px auto;
    display: flex;
    flex-wrap: wrap;
    gap: 33px 17px;
`